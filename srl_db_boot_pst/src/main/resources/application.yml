# Conversion to YAML from Properties formar report
# Warnings:
# - The yaml file had comments which are lost in the refactoring!
spring:
  application:
    name: srl_db_boot_pst
  datasource:
    hikari:
      connectionTimeout: 20000
      maximumPoolSize: 5
    url: jdbc:postgresql://ep-flat-poetry-a5o2edp9.us-east-2.aws.neon.tech:5432/srldb
    username: srldb_owner
    password: 4xzHhdFG9AIc
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
server:
  port: 8073
